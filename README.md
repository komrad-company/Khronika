# Khronika

We can see all things komrad

## Usage

Add to your project this line
`khronika = { git = "https://github.com/komrad-company/Khronika.git" }`

On your code, you must add the configuration and call on your main the function `initialize_logger`

now you can call `khronika::error!()` for example.
